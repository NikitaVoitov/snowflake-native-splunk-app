# ─────────────────────────────────────────────────────────────────
# environment.yml — Snowflake Anaconda Channel runtime dependencies
# All packages must be available on the Snowflake Anaconda Channel.
# Versions pinned to latest available as of 2026-03-15.
# Verified via: SELECT PACKAGE_NAME, MAX(VERSION) FROM INFORMATION_SCHEMA.PACKAGES
#               WHERE LANGUAGE = 'python' AND PACKAGE_NAME IN (...) GROUP BY 1;
# ─────────────────────────────────────────────────────────────────

name: sf_env
channels:
  - snowflake
dependencies:
  # Core Application Framework
  - streamlit=1.52.2
  - snowflake-snowpark-python=1.9.0
  - snowflake-native-apps-permission=0.1.9

  # Visualization & Dashboard
  - plotly=6.5.0

  # Telemetry Export (OTLP/gRPC)
  - opentelemetry-sdk=1.38.0
  - opentelemetry-exporter-otlp-proto-grpc=1.38.0
  - grpcio=1.78.0
  - protobuf=6.33.5

  # Data Serialization
  - orjson=3.9.15
