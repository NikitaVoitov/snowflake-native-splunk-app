# Epic 1 Retrospective — App foundation and installation

**Date:** 2026-03-17  
**Epic:** 1 — App foundation and installation  
**Project:** snowflake-native-splunk-app

---

## Epic outcome

Epic 1 is complete.

The app now installs successfully, supports privilege approval and warehouse binding, opens a working Streamlit shell in Snowsight, and includes the foundational config/state storage needed for the next phase. The navigation shell is in place, the About dialog works, and the install path has been validated in a real environment rather than only by static review.

**Delivery metrics**

| Metric | Value |
|---|---|
| Total stories | 3 |
| Stories done | 3 |
| Completion | 100% |

---

## What went well

1. **The foundation is real, not theoretical.**  
   The install path, warehouse binding, Streamlit shell, and core state tables were validated against actual Snowflake behavior. That matters because this epic was full of platform-specific behavior that is easy to misunderstand from docs alone.

2. **We corrected the most important gap before closing the epic.**  
   Reopening Story 1.1 to finish warehouse binding was the right call. Epic 1 should only be considered done when the app installs, binds a warehouse, and the Streamlit UI can actually run queries. That is now true.

3. **We have a usable shell for the next epic.**  
   The Splunk settings page stub, Getting Started route, About dialog, and page routing give Epic 2 a solid place to build instead of forcing another round of shell work.

4. **We developed good instincts for validating Snowflake Native App behavior.**  
   The strongest progress came from checking the app in its real runtime context: verifying packaged Streamlit assets on stage, checking the installed app behavior in Snowsight, and validating internal objects from the correct execution context.

---

## Friction and lessons

1. **Snowflake Native App behavior still has sharp edges.**  
   Artifact mapping, installed-app visibility, and Streamlit runtime behavior all required live verification. The lesson is simple: for this project, important platform assumptions should be proven in the target environment early.

2. **Provider-side metadata is not always the source of truth.**  
   Some app-internal objects are not reliably visible from the provider-side perspective, even when they exist and work correctly. For internal objects, app-context verification is sometimes the only dependable check.

3. **Repeat deploy checks should be intentional, not automatic.**  
   Re-run and idempotency checks were useful during foundation work, especially for setup and packaging changes. They should remain part of the toolkit, but only when a change actually touches install, setup, deploy, or object-creation behavior. They should not become a blanket requirement for every UI iteration because that slows feedback without adding much value.

---

## Epic 2 readiness

Epic 2 is not just form wiring. It introduces real external validation and needs real test assets.

### Story 2.1 — OTLP endpoint and connection test

This story needs a **real OTLP-capable endpoint** that can accept connections from Snowflake. A mock text field is not enough. The work should be prepared around:

- A reachable OTLP collector or equivalent endpoint that supports **gRPC**.
- Real connection checks from the app to that endpoint, not just string validation.
- The Snowflake-side objects required for outbound connectivity, such as the appropriate network rules and related egress configuration.
- Target-side network access that allows inbound gRPC traffic from Snowflake to reach the collector successfully.

### Story 2.2 — Optional certificate and validation

This story needs a **real PEM certificate** for validation work. The checks should go beyond basic parsing and cover practical certificate validation concerns that matter for a real destination:

- PEM structure is valid.
- Certificate validity window is current.
- Hostname usage is compatible with the configured endpoint.
- The certificate is appropriate for the intended TLS use.

### Story 2.3 — Persist destination config and Getting Started hub

This story should deliver the **full Getting Started experience**, not a placeholder. That means:

- Task tiles for the four setup steps.
- Progress state and visual completion feedback.
- Drill-down behavior from tasks into the correct page or flow.
- Persistent completion state after Maya saves valid destination settings.

Because FR3 is delivered through Epic 2, Story 2.3 should leave the app with a **workable Getting Started page**, not just layout scaffolding. For tasks that depend on stories from later epics, Story 2.3 can use temporary UI placeholders for state progression so the page behaves like a real onboarding hub while the downstream features are still being built. A practical approach is:

- **Splunk settings** must be completed for real through the actual connection and save flow.
- The other tasks can use temporary state controls in Story 2.3, such as checkboxes or equivalent UI toggles, so their completion state updates the sidebar counter and the visual state of the Getting Started page.
- These temporary controls should be easy to replace later with real completion signals once the corresponding stories are implemented.

This note should be carried forward when Story 2.3 is created so the implementation intentionally delivers a usable onboarding experience in Epic 2 while remaining compatible with later epics.

Epic 2 therefore has external preparation work in addition to UI implementation. The foundation from Epic 1 is ready, but Epic 2 still needs real integration targets and real validation assets before it can be considered fully testable.

An additional advantage for Epic 2 is that development and troubleshooting can use direct access to the target collector environment. SSH access to the OTLP/gRPC-capable endpoint will make it possible to inspect collector logs and use OS-level CLI tools while validating real connection behavior during development.

---

## Action items

| Action | Owner | Status | Notes |
|---|---|---|---|
| Prepare a real OTLP/gRPC-capable target for Story 2.1 testing | Nik / Dev | **Done** | Splunk OTEL Collector deployed with TLS-enabled gRPC receiver. Verified with probe. See [OTLP collector details](#otlp-collector-connection-details). |
| Configure the Snowflake-side outbound connectivity objects and target-side inbound network access needed for OTLP/gRPC testing | Nik / Dev | **Pending** | Target-side inbound access is confirmed open. Snowflake-side external access integration and network rules still needed when Epic 2 stories begin. |
| Prepare a real PEM certificate for Story 2.2 | Nik / Dev | **Done** | Self-signed CA + server cert created. PEM validator built and exercised against valid, invalid, and wrong-CA certs. See [TLS cert setup](#tls-certificate-setup) and [PEM validation tooling](#pem-validation-tooling). |
| Use SSH access to the collector host during Epic 2 development for log inspection and CLI-based verification | Nik / Dev | **Ready** | SSH alias `otelcol` configured and in use. See [OTLP collector details](#otlp-collector-connection-details). |

---

## OTLP collector connection details

This section gives the AI agent (and the developer) everything needed to work with the real OTLP test target during Epic 2.

### Endpoint

| Property | Value |
|---|---|
| OTLP gRPC endpoint | `otelcol.israelcentral.cloudapp.azure.com:4317` |
| Protocol | gRPC over TLS (plaintext rejected) |
| SSH alias | `otelcol` (configured in `~/.ssh/config`) |

### Collector runtime

| Property | Value |
|---|---|
| Collector | Splunk OpenTelemetry Collector (`splunk-otel-collector`) |
| Config file | `/etc/otel/collector/agent_config.yaml` |
| TLS cert | `/etc/otel/collector/tls/server.crt` |
| TLS key | `/etc/otel/collector/tls/server.key` |
| Service management | `sudo systemctl restart splunk-otel-collector` |
| Logs | `sudo journalctl -u splunk-otel-collector -n 300` |

### Collector OTLP receiver config (TLS-enabled)

```yaml
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "${SPLUNK_LISTEN_INTERFACE}:4317"
        tls:
          cert_file: /etc/otel/collector/tls/server.crt
          key_file: /etc/otel/collector/tls/server.key
      http:
        endpoint: "${SPLUNK_LISTEN_INTERFACE}:4318"
```

### Connecting from local dev

```bash
# TLS probe (should succeed)
uv run python grpc_test/otlp_grpc_probe.py otelcol.israelcentral.cloudapp.azure.com:4317 \
  --tls --pem grpc_test/tls-setup/ca.crt -v

# Diagnose mode (plaintext vs TLS comparison)
uv run python grpc_test/otlp_grpc_probe.py otelcol.israelcentral.cloudapp.azure.com:4317 \
  --diagnose --pem grpc_test/tls-setup/ca.crt

# openssl verification
openssl s_client -connect otelcol.israelcentral.cloudapp.azure.com:4317 \
  -CAfile grpc_test/tls-setup/ca.crt -brief
```

### Key behavior observed

- **Plaintext connections are rejected.** The collector's OTLP gRPC receiver requires TLS. Plaintext probes show `IDLE -> CONNECTING -> TRANSIENT_FAILURE` and time out.
- **TLS with the correct CA succeeds.** Probe shows `IDLE -> CONNECTING -> READY`.
- **TLS with a wrong CA fails with `CERTIFICATE_VERIFY_FAILED`.** The `--diagnose` mode and the SSL cert check both surface this clearly.

---

## TLS certificate setup

### What was generated

Self-signed CA and server certificate created under `grpc_test/tls-setup/` using `generate-certs.sh`.

| File | Purpose | Location |
|---|---|---|
| `ca.crt` | CA certificate (trust anchor for probe `--pem`) | `grpc_test/tls-setup/ca.crt` |
| `ca.key` | CA private key (gitignored) | `grpc_test/tls-setup/ca.key` |
| `server.crt` | Server certificate for collector (SAN: `otelcol.israelcentral.cloudapp.azure.com`) | `grpc_test/tls-setup/server.crt` |
| `server.key` | Server private key (gitignored, deployed to collector) | `grpc_test/tls-setup/server.key` |
| `wrong_ca.crt` | Unrelated CA for negative testing | `grpc_test/tls-setup/wrong_ca.crt` |

### CA properties

- Subject: `CN=OTLP Test CA`
- BasicConstraints: `CA:TRUE`
- KeyUsage: `keyCertSign, crlSign`
- Validity: 1 year from 2026-03-18

### Server cert properties

- SAN: `DNS:otelcol.israelcentral.cloudapp.azure.com`
- Signed by the test CA above

### Deployment

Certs were deployed to the collector via SSH:

```bash
scp server.crt server.key otelcol:/tmp/
ssh otelcol "sudo mkdir -p /etc/otel/collector/tls && sudo mv /tmp/server.crt /tmp/server.key /etc/otel/collector/tls/ && sudo chown splunk-otel-collector:splunk-otel-collector /etc/otel/collector/tls/*"
```

Full setup instructions: `grpc_test/tls-setup/README.md`

---

## gRPC probe tooling

### Tool: `grpc_test/otlp_grpc_probe.py`

Implements Approaches A and B from `grpc_connectivity_testing_nites.md` plus a `--diagnose` mode for distinguishing failure root causes.

#### Modes

| Mode | Flag | What it does |
|---|---|---|
| Approach A | `--approach a` | Wait for channel `READY`, no state logging |
| Approach B (default) | `--approach b` | Wait for `READY` + record state transitions |
| Diagnose | `--diagnose --pem <ca.pem>` | Run plaintext and TLS probes, compare results, run SSL cert check for actionable error messages |

#### Diagnostic capabilities

The probe can distinguish these failure categories:

| Symptom | Diagnosis |
|---|---|
| Plaintext fails (`TRANSIENT_FAILURE`), TLS succeeds | Server requires TLS |
| Both fail, SSL check returns `CERTIFICATE_VERIFY_FAILED` | Wrong CA or cert chain |
| Both fail, SSL check returns hostname mismatch | Server cert CN/SAN does not match endpoint |
| Both fail, timeout in `CONNECTING` | Network / firewall / port issue |
| TLS fails with `PEM_INVALID` | Malformed PEM input |

#### Key CLI examples

```bash
# TLS with correct CA
uv run python grpc_test/otlp_grpc_probe.py otelcol.israelcentral.cloudapp.azure.com:4317 \
  --tls --pem grpc_test/tls-setup/ca.crt -v

# Diagnose (plaintext vs TLS comparison)
uv run python grpc_test/otlp_grpc_probe.py otelcol.israelcentral.cloudapp.azure.com:4317 \
  --diagnose --pem grpc_test/tls-setup/ca.crt

# Wrong CA (shows CERTIFICATE_VERIFY_FAILED diagnostic)
uv run python grpc_test/otlp_grpc_probe.py otelcol.israelcentral.cloudapp.azure.com:4317 \
  --tls --pem grpc_test/tls-setup/wrong_ca.crt

# Plaintext (expected to fail on TLS-only server, shows hint)
uv run python grpc_test/otlp_grpc_probe.py otelcol.israelcentral.cloudapp.azure.com:4317 --timeout 3
```

---

## PEM validation tooling

### Tool: `grpc_test/pem_validator.py`

Implements Section 7 (PEM handling strategy) from `grpc_connectivity_testing_nites.md`. Uses `cryptography` for offline X.509 parsing.

#### What it validates

- PEM structure (at least one certificate present)
- Time validity (not expired, not yet valid)
- CA trust anchor checks (`BasicConstraints CA=TRUE`, `KeyUsage keyCertSign`)
- Certificate metadata (Subject, Issuer, Fingerprint, Serial, SAN)

#### Key CLI examples

```bash
# Validate CA cert (verbose)
uv run python grpc_test/pem_validator.py grpc_test/tls-setup/ca.crt -v

# Validate server cert (skip CA check)
uv run python grpc_test/pem_validator.py grpc_test/tls-setup/server.crt --no-ca-check

# Server cert with CA check (expected to fail — not a CA)
uv run python grpc_test/pem_validator.py grpc_test/tls-setup/server.crt

# Stdin
cat grpc_test/tls-setup/ca.crt | uv run python grpc_test/pem_validator.py -
```

---

## Readiness assessment

| Area | Status | Notes |
|---|---|---|
| Epic 1 implementation | Ready | Story 1.1 has been validated and is now marked done |
| Installed app behavior | Ready | Install, warehouse binding, and Streamlit shell work together |
| Config/state foundation | Ready | Durable tables and grants are in place for Epic 2 |
| Epic 2 UI starting point | Ready | Splunk settings and Getting Started routes exist and can now be expanded |
| OTLP/gRPC target | **Ready** | Collector deployed with TLS, verified by probe and openssl |
| PEM test material | **Ready** | CA cert, server cert, and wrong CA available; PEM validator built and tested |
| Collector SSH access | **Ready** | SSH alias `otelcol` active; logs and config inspectable |
| gRPC probe tooling | **Ready** | Approaches A/B + diagnose mode with cert-error diagnostics |
| Snowflake outbound network config | **Needs work** | External access integration and network rules not yet created; required for Story 2.1 in-app testing |

---

## Next steps

1. Start Epic 2 with real integration prep mostly complete — the collector, certs, probes, and SSH access are all in place.
2. **Story 2.1** can now use the existing probe logic as a reference (possible) implementation when building the in-app stored procedure. The stored procedure should mirror the same `grpcio` channel readiness pattern, structured diagnostics, and the `cryptography`-based PEM validation already proven in the CLI tools.
3. **Story 2.2** has real PEM test material ready (`ca.crt`, `server.crt`, `wrong_ca.crt`) and a working validator to use as reference.
4. **Remaining prep:** Create the Snowflake external access integration and network rules for `otelcol.israelcentral.cloudapp.azure.com:4317` before Story 2.1 can run the probe from inside Snowflake.
5. Use `ssh otelcol` and `sudo journalctl -u splunk-otel-collector` to inspect collector-side behavior during development.
6. **Story 2.3** delivers the full Getting Started page with tasks, progress, and persistent completion state.

---

## Reference files

| File | Purpose |
|---|---|
| `grpc_test/otlp_grpc_probe.py` | gRPC connectivity probe (Approaches A/B + diagnose mode) |
| `grpc_test/pem_validator.py` | PEM/X.509 offline validation |
| `grpc_test/grpc_connectivity_testing_nites.md` | Design notes (all approaches, tradeoffs, architecture) |
| `grpc_test/tls-setup/README.md` | TLS cert generation and collector setup instructions |
| `grpc_test/tls-setup/ca.crt` | CA certificate for probe `--pem` |
| `grpc_test/tls-setup/server.crt` | Server cert deployed to collector |
| `grpc_test/tls-setup/wrong_ca.crt` | Unrelated CA for negative testing |
| `grpc_test/tls-setup/generate-certs.sh` | Cert generation script |

---

*Focus remains on systems, real validation, and shipping the next epic with fewer assumptions. The first three action items from the original retro have been addressed — the OTLP target, TLS certs, and probe tooling are ready for Epic 2.*
